import XCTest
@testable import SPMSampleProject

final class SPMSampleProjectTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SPMSampleProject().text, "Hello, World!")
    }
}
